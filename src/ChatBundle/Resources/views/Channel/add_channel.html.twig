{% extends "::base.html.twig" %}

{% block title %}ChatBundle:Channel:AddChannel{% endblock %}

{% block body %}
<h1>Welcome to the Channel:AddChannel page</h1>
    <div class="container">
        <div class="row row">
            <div class="row">
                {{ form_start(form) }}
                <div class="input-field col s6">
                    {{ form_label (form.name) }}
                    {{ form_widget (form.name) }}
                    {{ form_errors (form.name) }}
                </div>
            </div>
            <div class="row">
                <div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
